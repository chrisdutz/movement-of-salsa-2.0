// REMARK: This "type" tells the compiler to only take the type and not the rest.
import type {User} from "../generated/plc4j-tools-ui-frontend.ts";
import {ApplicationModule} from "../utils/ApplicationModule.ts";

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// Authentication related
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

export type SetAuthenticationTokenAction = {
    authToken: string;
}

export type SetAuthenticationUserAction = {
    user: User;
}

export type AuthenticationState = {
    authToken?: string
    user?: User
}

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// Module related
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

export type SetModulesAction = {
    modules: ApplicationModule[];
}

export type ModulesState = {
    modules?: ApplicationModule[];
}

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// Store related
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

export interface BaseStore {
    authentication: AuthenticationState,
    modules: ModulesState
}
